NODE(1)                     General Commands Manual                    NODE(1)

NNAAMMEE
     nnooddee – server-side JavaScript runtime

SSYYNNOOPPSSIISS
     nnooddee [_o_p_t_i_o_n_s] [_v_8_-_o_p_t_i_o_n_s] [--ee _s_t_r_i_n_g | _s_c_r_i_p_t_._j_s | --] [----]
          [_a_r_g_u_m_e_n_t_s _._._.]
     nnooddee iinnssppeecctt [--ee _s_t_r_i_n_g | _s_c_r_i_p_t_._j_s | -- | _<_h_o_s_t_>_:_<_p_o_r_t_>] _._._.
     nnooddee [----vv88--ooppttiioonnss]

DDEESSCCRRIIPPTTIIOONN
     Node.js is a set of libraries for JavaScript which allows it to be used
     outside of the browser.  It is primarily focused on creating simple,
     easy-to-build network clients and servers.

     Execute nnooddee without arguments to start a REPL.

OOPPTTIIOONNSS
     --       Alias for stdin, analogous to the use of - in other command-line
             utilities.  The executed script is read from stdin, and remaining
             arguments are passed to the script.

     ----      Indicate the end of command-line options.  Pass the rest of the
             arguments to the script.

             If no script filename or eval/print script is supplied prior to
             this, then the next argument will be used as a script filename.

     ----aabboorrtt--oonn--uunnccaauugghhtt--eexxcceeppttiioonn
             Aborting instead of exiting causes a core file to be generated
             for analysis.

     ----aallllooww--ffss--rreeaadd
             Allow file system read access when using the permission model.

     ----aallllooww--ffss--wwrriittee
             Allow file system write access when using the permission model.

     ----aallllooww--cchhiilldd--pprroocceessss
             Allow spawning process when using the permission model.

     ----aallllooww--wwoorrkkeerr
             Allow creating worker threads when using the permission model.

     ----ccoommpplleettiioonn--bbaasshh
             Print source-able bash completion script for Node.js.

     --CC, ----ccoonnddiittiioonnss _s_t_r_i_n_g
             Use custom conditional exports conditions.  _s_t_r_i_n_g

     ----ccppuu--pprrooff
             Start the V8 CPU profiler on start up, and write the CPU profile
             to disk before exit. If ----ccppuu--pprrooff--ddiirr is not specified, the
             profile will be written to the current working directory with a
             generated file name.

     ----ccppuu--pprrooff--ddiirr
             The directory where the CPU profiles generated by ----ccppuu--pprrooff will
             be placed.  The default value is controlled by the
             ----ddiiaaggnnoossttiicc--ddiirr.  command-line option.

     ----ccppuu--pprrooff--iinntteerrvvaall
             The sampling interval in microseconds for the CPU profiles
             generated by ----ccppuu--pprrooff.  The default is 11000000.

     ----ccppuu--pprrooff--nnaammee
             File name of the V8 CPU profile generated with ----ccppuu--pprrooff.

     ----ddiiaaggnnoossttiicc--ddiirr
             Set the directory for all diagnostic output files.  Default is
             current working directory.  Set the directory to which all
             diagnostic output files will be written to.  Defaults to current
             working directory.  Affects the default output directory of:
             ----ccppuu--pprrooff--ddiirr.  ----hheeaapp--pprrooff--ddiirr.  ----rreeddiirreecctt--wwaarrnniinnggss.

     ----ddiissaabbllee--pprroottoo=_m_o_d_e
             Disable the `Object.prototype.__proto__` property. If _m_o_d_e is
             `delete`, the property will be removed entirely. If _m_o_d_e is
             `throw`, accesses to the property will throw an exception with
             the code `ERR_PROTO_ACCESS`.

     ----ddiissaallllooww--ccooddee--ggeenneerraattiioonn--ffrroomm--ssttrriinnggss
             Make built-in language features like `eval` and `new Function`
             that generate code from strings throw an exception instead. This
             does not affect the Node.js `vm` module.

     ----eennaabbllee--ffiippss
             Enable FIPS-compliant crypto at startup.  Requires Node.js to be
             built with ..//ccoonnffiigguurree ----ooppeennssssll--ffiippss.

     ----eennaabbllee--ssoouurrccee--mmaappss
             Enable Source Map V3 support for stack traces.

     ----eexxppeerriimmeennttaall--gglloobbaall--wweebbccrryyppttoo
             Expose the Web Crypto API on the global scope.

     ----eexxppeerriimmeennttaall--iimmppoorrtt--mmeettaa--rreessoollvvee
             Enable experimental ES modules support for import.meta.resolve().

     ----eexxppeerriimmeennttaall--llooaaddeerr=_m_o_d_u_l_e
             Specify the _m_o_d_u_l_e to use as a custom module loader.

     ----eexxppeerriimmeennttaall--nneettwwoorrkk--iimmppoorrttss
             Enable experimental support for loading modules using `import`
             over `https:`.

     ----eexxppeerriimmeennttaall--ppeerrmmiissssiioonn
             Enable the experimental permission model.

     ----eexxppeerriimmeennttaall--ppoolliiccyy
             Use the specified file as a security policy.

     ----eexxppeerriimmeennttaall--sshhaaddooww--rreeaallmm
             Use this flag to enable ShadowRealm support.

     ----eexxppeerriimmeennttaall--tteesstt--ccoovveerraaggee
             Enable code coverage in the test runner.

     ----nnoo--eexxppeerriimmeennttaall--ffeettcchh
             Disable experimental support for the Fetch API.

     ----nnoo--eexxppeerriimmeennttaall--gglloobbaall--ccuussttoommeevveenntt
             Disable exposition of the CustomEvent on the global scope.

     ----nnoo--eexxppeerriimmeennttaall--gglloobbaall--wweebbccrryyppttoo
             Disable exposition of the Web Crypto API on the global scope.

     ----nnoo--eexxppeerriimmeennttaall--rreeppll--aawwaaiitt
             Disable top-level await keyword support in REPL.

     ----eexxppeerriimmeennttaall--vvmm--mmoodduulleess
             Enable experimental ES module support in VM module.

     ----eexxppeerriimmeennttaall--wwaassii--uunnssttaabbllee--pprreevviieeww11
             Enable experimental WebAssembly System Interface support. This
             flag is no longer required as WASI is enabled by default.

     ----eexxppeerriimmeennttaall--wwaassmm--mmoodduulleess
             Enable experimental WebAssembly module support.

     ----ffoorrccee--ccoonntteexxtt--aawwaarree
             Disable loading native addons that are not context-aware.

     ----ffoorrccee--ffiippss
             Force FIPS-compliant crypto on startup (Cannot be disabled from
             script code).  Same requirements as ----eennaabbllee--ffiippss.

     ----ffrroozzeenn--iinnttrriinnssiiccss
             Enable experimental frozen intrinsics support.

     ----hheeaappssnnaappsshhoott--nneeaarr--hheeaapp--lliimmiitt=_m_a_x___c_o_u_n_t
             Generate heap snapshot when the V8 heap usage is approaching the
             heap limit.  No more than the specified number of snapshots will
             be generated.

     ----hheeaappssnnaappsshhoott--ssiiggnnaall=_s_i_g_n_a_l
             Generate heap snapshot on specified signal.

     ----hheeaapp--pprrooff
             Start the V8 heap profiler on start up, and write the heap
             profile to disk before exit. If ----hheeaapp--pprrooff--ddiirr is not specified,
             the profile will be written to the current working directory with
             a generated file name.

     ----hheeaapp--pprrooff--ddiirr
             The directory where the heap profiles generated by ----hheeaapp--pprrooff
             will be placed.  The default value is controlled by the
             ----ddiiaaggnnoossttiicc--ddiirr.  command-line option.

     ----hheeaapp--pprrooff--iinntteerrvvaall
             The average sampling interval in bytes for the heap profiles
             generated by ----hheeaapp--pprrooff.  The default is 551122 ** 11002244.

     ----hheeaapp--pprrooff--nnaammee
             File name of the V8 heap profile generated with ----hheeaapp--pprrooff.

     ----iiccuu--ddaattaa--ddiirr=_f_i_l_e
             Specify ICU data load path.  Overrides NODE_ICU_DATA.

     ----iinnppuutt--ttyyppee=_t_y_p_e
             Set the module resolution type for input via --eval, --print or
             STDIN.

     ----iinnssppeecctt--bbrrkk=_[_h_o_s_t_:_]_p_o_r_t
             Activate inspector on _h_o_s_t_:_p_o_r_t and break at start of user
             script.

     ----iinnssppeecctt--ppoorrtt=_[_h_o_s_t_:_]_p_o_r_t
             Set the _h_o_s_t_:_p_o_r_t to be used when the inspector is activated.

     ----iinnssppeecctt--ppuubblliisshh--uuiidd==ssttddeerrrr,,hhttttpp
             Specify how the inspector WebSocket URL is exposed.  Valid values
             are ssttddeerrrr and hhttttpp.  Default is ssttddeerrrr,,hhttttpp.

     ----iinnssppeecctt=_[_h_o_s_t_:_]_p_o_r_t
             Activate inspector on _h_o_s_t_:_p_o_r_t.  Default is 112277..00..00..11::99222299.

             V8 Inspector integration allows attaching Chrome DevTools and
             IDEs to Node.js instances for debugging and profiling.  It uses
             the Chrome DevTools Protocol.

     ----iinnsseeccuurree--hhttttpp--ppaarrsseerr
             Use an insecure HTTP parser that accepts invalid HTTP headers.
             This may allow interoperability with non-conformant HTTP
             implementations. It may also allow request smuggling and other
             HTTP attacks that rely on invalid headers being accepted. Avoid
             using this option.

     ----jjiittlleessss
             Disable runtime allocation of executable memory. This may be
             required on some platforms for security reasons. It can also
             reduce attack surface on other platforms, but the performance
             impact may be severe.

             This flag is inherited from V8 and is subject to change upstream.
             It may disappear in a non-semver-major release.

     ----mmaaxx--hhttttpp--hheeaaddeerr--ssiizzee=_s_i_z_e
             Specify the maximum size of HTTP headers in bytes. Defaults to 16
             KiB.

     ----nnaappii--mmoodduulleess
             This option is a no-op.  It is kept for compatibility.

     ----nnoo--ddeepprreeccaattiioonn
             Silence deprecation warnings.

     ----nnoo--eexxttrraa--iinnffoo--oonn--ffaattaall--eexxcceeppttiioonn
             Hide extra information on fatal exception that causes exit.

     ----nnoo--ffoorrccee--aassyynncc--hhooookkss--cchheecckkss
             Disable runtime checks for `async_hooks`.  These will still be
             enabled dynamically when `async_hooks` is enabled.

     ----nnoo--aaddddoonnss
             Disable the `node-addons` exports condition as well as disable
             loading native addons. When `--no-addons` is specified, calling
             `process.dlopen` or requiring a native C++ addon will fail and
             throw an exception.

     ----nnoo--gglloobbaall--sseeaarrcchh--ppaatthhss
             Do not search modules from global paths.

     ----nnoo--wwaarrnniinnggss
             Silence all process warnings (including deprecations).

     ----nnooddee--mmeemmoorryy--ddeebbuugg
             Enable extra debug checks for memory leaks in Node.js internals.
             This is usually only useful for developers debugging Node.js
             itself.

     ----ooppeennssssll--ccoonnffiigg=_f_i_l_e
             Load an OpenSSL configuration file on startup.  Among other uses,
             this can be used to enable FIPS-compliant crypto if Node.js is
             built with ..//ccoonnffiigguurree ----ooppeennssssll--ffiippss.

     ----ppeennddiinngg--ddeepprreeccaattiioonn
             Emit pending deprecation warnings.

     ----ppoolliiccyy--iinntteeggrriittyy=_s_r_i
             Instructs Node.js to error prior to running any code if the
             policy does not have the specified integrity. It expects a
             Subresource Integrity string as a parameter.

     ----pprreesseerrvvee--ssyymmlliinnkkss
             Instructs the module loader to preserve symbolic links when
             resolving and caching modules other than the main module.

     ----pprreesseerrvvee--ssyymmlliinnkkss--mmaaiinn
             Instructs the module loader to preserve symbolic links when
             resolving and caching the main module.

     ----pprrooff  Generate V8 profiler output.

     ----pprrooff--pprroocceessss
             Process V8 profiler output generated using the V8 option ----pprrooff.

     ----rreeddiirreecctt--wwaarrnniinnggss=_f_i_l_e
             Write process warnings to the given _f_i_l_e instead of printing to
             stderr.

     ----rreeppoorrtt--ccoommppaacctt
             Write ddiiaaggnnoossttiicc rreeppoorrttss in a compact format, single-line JSON.

     ----rreeppoorrtt--ddiirr ----rreeppoorrtt--ddiirreeccttoorryy
             Location at which the ddiiaaggnnoossttiicc rreeppoorrtt will be generated.  The
             `file` name may be an absolute path. If it is not, the default
             directory it will be written to is controlled by the
             ----ddiiaaggnnoossttiicc--ddiirr.  command-line option.

     ----rreeppoorrtt--ffiilleennaammee
             Name of the file to which the ddiiaaggnnoossttiicc rreeppoorrtt will be written.

     ----rreeppoorrtt--oonn--ffaattaalleerrrroorr
             Enables the ddiiaaggnnoossttiicc rreeppoorrtt to be triggered on fatal errors
             (internal errors within the Node.js runtime such as out of
             memory) that leads to termination of the application. Useful to
             inspect various diagnostic data elements such as heap, stack,
             event loop state, resource consumption etc. to reason about the
             fatal error.

     ----rreeppoorrtt--oonn--ssiiggnnaall
             Enables ddiiaaggnnoossttiicc rreeppoorrtt to be generated upon receiving the
             specified (or predefined) signal to the running Node.js process.
             Default signal is SIGUSR2.

     ----rreeppoorrtt--ssiiggnnaall
             Sets or resets the signal for ddiiaaggnnoossttiicc rreeppoorrtt generation (not
             supported on Windows). Default signal is SIGUSR2.

     ----rreeppoorrtt--uunnccaauugghhtt--eexxcceeppttiioonn
             Enables ddiiaaggnnoossttiicc rreeppoorrtt to be generated on un-caught
             exceptions. Useful when inspecting JavaScript stack in
             conjunction with native stack and other runtime environment data.

     ----sseeccuurree--hheeaapp=_n
             Specify the size of the OpenSSL secure heap. Any value less than
             2 disables the secure heap. The default is 0. The value must be a
             power of two.

     ----sseeccuurree--hheeaapp--mmiinn=_n
             Specify the minimum allocation from the OpenSSL secure heap. The
             default is 2. The value must be a power of two.

     ----tteesstt  Starts the Node.js command line test runner.

     ----tteesstt--nnaammee--ppaatttteerrnn
             A regular expression that configures the test runner to only
             execute tests whose name matches the provided pattern.

     ----tteesstt--rreeppoorrtteerr
             A test reporter to use when running tests.

     ----tteesstt--rreeppoorrtteerr--ddeessttiinnaattiioonn
             The destination for the corresponding test reporter.

     ----tteesstt--oonnllyy
             Configures the test runner to only execute top level tests that
             have the `only` option set.

     ----tthhrrooww--ddeepprreeccaattiioonn
             Throw errors for deprecations.

     ----ttiittllee=_t_i_t_l_e
             Specify process.title on startup.

     ----ttllss--cciipphheerr--lliisstt=_l_i_s_t
             Specify an alternative default TLS cipher list.  Requires Node.js
             to be built with crypto support. (Default)

     ----ttllss--kkeeyylloogg=_f_i_l_e
             Log TLS key material to a file. The key material is in NSS
             SSLKEYLOGFILE format and can be used by software (such as
             Wireshark) to decrypt the TLS traffic.

     ----ttllss--mmaaxx--vv11..22
             Set default  maxVersion to 'TLSv1.2'. Use to disable support for
             TLSv1.3.

     ----ttllss--mmaaxx--vv11..33
             Set default  maxVersion to 'TLSv1.3'. Use to enable support for
             TLSv1.3.

     ----ttllss--mmiinn--vv11..00
             Set default minVersion to 'TLSv1'. Use for compatibility with old
             TLS clients or servers.

     ----ttllss--mmiinn--vv11..11
             Set default minVersion to 'TLSv1.1'. Use for compatibility with
             old TLS clients or servers.

     ----ttllss--mmiinn--vv11..22
             Set default minVersion to 'TLSv1.2'. This is the default for 12.x
             and later, but the option is supported for compatibility with
             older Node.js versions.

     ----ttllss--mmiinn--vv11..33
             Set default minVersion to 'TLSv1.3'. Use to disable support for
             TLSv1.2 in favour of TLSv1.3, which is more secure.

     ----ttrraaccee--aattoommiiccss--wwaaiitt
             Print short summaries of calls to AAttoommiiccss..wwaaiitt(()).  This flag is
             deprecated.

     ----ttrraaccee--ddeepprreeccaattiioonn
             Print stack traces for deprecations.

     ----ttrraaccee--eevveenntt--ccaatteeggoorriieess _c_a_t_e_g_o_r_i_e_s
             A comma-separated list of categories that should be traced when
             trace event tracing is enabled using ----ttrraaccee--eevveennttss--eennaabblleedd.

     ----ttrraaccee--eevveenntt--ffiillee--ppaatttteerrnn _p_a_t_t_e_r_n
             Template string specifying the filepath for the trace event data,
             it supports $${{rroottaattiioonn}} and $${{ppiidd}}.

     ----ttrraaccee--eevveennttss--eennaabblleedd
             Enable the collection of trace event tracing information.

     ----ttrraaccee--eexxiitt
             Prints a stack trace whenever an environment is exited
             proactively, i.e. invoking `process.exit()`.

     ----ttrraaccee--ssiiggiinntt
             Prints a stack trace on SIGINT.

     ----ttrraaccee--ssyynncc--iioo
             Print a stack trace whenever synchronous I/O is detected after
             the first turn of the event loop.

     ----ttrraaccee--ttllss
             Prints TLS packet trace information to stderr.

     ----ttrraaccee--uunnccaauugghhtt
             Print stack traces for uncaught exceptions; usually, the stack
             trace associated with the creation of an EErrrroorr is printed,
             whereas this makes Node.js also print the stack trace associated
             with throwing the value (which does not need to be an EErrrroorr
             instance).

             Enabling this option may affect garbage collection behavior
             negatively.

     ----ttrraaccee--wwaarrnniinnggss
             Print stack traces for process warnings (including deprecations).

     ----ttrraacckk--hheeaapp--oobbjjeeccttss
             Track heap object allocations for heap snapshots.

     ----uunnhhaannddlleedd--rreejjeeccttiioonnss==mmooddee
             Define the behavior for unhandled rejections. Can be one of
             `strict` (raise an error), `warn` (enforce warnings) or `none`
             (silence warnings).

     ----uussee--bbuunnddlleedd--ccaa, ----uussee--ooppeennssssll--ccaa
             Use bundled Mozilla CA store as supplied by current Node.js
             version or use OpenSSL's default CA store.  The default store is
             selectable at build-time.

             The bundled CA store, as supplied by Node.js, is a snapshot of
             Mozilla CA store that is fixed at release time.  It is identical
             on all supported platforms.

             Using OpenSSL store allows for external modifications of the
             store.  For most Linux and BSD distributions, this store is
             maintained by the distribution maintainers and system
             administrators.  OpenSSL CA store location is dependent on
             configuration of the OpenSSL library but this can be altered at
             runtime using environment variables.

             See SSL_CERT_DIR and SSL_CERT_FILE.

     ----uussee--llaarrggeeppaaggeess=_m_o_d_e
             Re-map the Node.js static code to large memory pages at startup.
             If supported on the target system, this will cause the Node.js
             static code to be moved onto 2 MiB pages instead of 4 KiB pages.

             _m_o_d_e must have one of the following values: `off` (the default
             value, meaning do not map), `on` (map and ignore failure,
             reporting it to stderr), or `silent` (map and silently ignore
             failure).

     ----vv88--ooppttiioonnss
             Print V8 command-line options.

     ----vv88--ppooooll--ssiizzee=_n_u_m
             Set V8's thread pool size which will be used to allocate
             background jobs.  If set to 0 then V8 will choose an appropriate
             size of the thread pool based on the number of online processors.
             If the value provided is larger than V8's maximum, then the
             largest value will be chosen.

     ----zzeerroo--ffiillll--bbuuffffeerrss
             Automatically zero-fills all newly allocated Buffer and
             SlowBuffer instances.

     --cc, ----cchheecckk
             Check the script's syntax without executing it.  Exits with an
             error code if script is invalid.

     --ee, ----eevvaall _s_t_r_i_n_g
             Evaluate _s_t_r_i_n_g as JavaScript.

     --hh, ----hheellpp
             Print command-line options.  The output of this option is less
             detailed than this document.

     --ii, ----iinntteerraaccttiivvee
             Open the REPL even if stdin does not appear to be a terminal.

     --pp, ----pprriinntt _s_t_r_i_n_g
             Identical to --ee, but prints the result.

     --rr, ----rreeqquuiirree _m_o_d_u_l_e
             Preload the specified _m_o_d_u_l_e at startup.  Follows `require()`'s
             module resolution rules.  _m_o_d_u_l_e may be either a path to a file,
             or a Node.js module name.

     --vv, ----vveerrssiioonn
             Print node's version.

EENNVVIIRROONNMMEENNTT
     FORCE_COLOR
             Used to enable ANSI colorized output. The value may be one of: _1
             , _t_r_u_e , or _a_n _e_m_p_t_y _s_t_r_i_n_g to indicate 16-color support, _2 to
             indicate 256-color support, or _3 to indicate 16 million-color
             support. When used and set to a supported value, both the
             NO_COLOR and NODE_DISABLE_COLORS environment variables are
             ignored. Any other value will result in colorized output being
             disabled.

     NO_COLOR
             Alias for NODE_DISABLE_COLORS

     NODE_DEBUG _m_o_d_u_l_e_s_._._.
             Comma-separated list of core modules that should print debug
             information.

     NODE_DEBUG_NATIVE _m_o_d_u_l_e_s_._._.
             Comma-separated list of C++ core modules that should print debug
             information.

     NODE_DISABLE_COLORS
             When set to _1, colors will not be used in the REPL.

     NODE_EXTRA_CA_CERTS _f_i_l_e
             When set, the well-known “root” CAs (like VeriSign) will be
             extended with the extra certificates in _f_i_l_e.  The file should
             consist of one or more trusted certificates in PEM format.

             If _f_i_l_e is missing or misformatted, a message will be emitted
             once using pprroocceessss..eemmiittWWaarrnniinngg(()), but any errors are otherwise
             ignored.

             This environment variable is ignored when `node` runs as setuid
             root or has Linux file capabilities set.

             The _N_O_D_E___E_X_T_R_A___C_A___C_E_R_T_S environment variable is only read when
             the Node.js process is first launched.  Changing the value at
             runtime using _p_r_o_c_e_s_s_._e_n_v_._N_O_D_E___E_X_T_R_A___C_A___C_E_R_T_S has no effect on
             the current process.

     NODE_ICU_DATA _f_i_l_e
             Data path for ICU (Intl object) data.  Will extend linked-in data
             when compiled with small-icu support.

     NODE_NO_WARNINGS
             When set to _1, process warnings are silenced.

     NODE_OPTIONS _o_p_t_i_o_n_s_._._.
             A space-separated list of command-line _o_p_t_i_o_n_s, which are
             interpreted as if they had been specified on the command line
             before the actual command (so they can be overridden).  Node.js
             will exit with an error if an option that is not allowed in the
             environment is used, such as ----pprriinntt or a script file.

     NODE_PATH _d_i_r_e_c_t_o_r_i_e_s_._._.
             A colon-separated list of _d_i_r_e_c_t_o_r_i_e_s prefixed to the module
             search path.

     NODE_PENDING_DEPRECATION
             When set to _1, emit pending deprecation warnings.

     NODE_PRESERVE_SYMLINKS
             When set to _1, the module loader preserves symbolic links when
             resolving and caching modules.

     NODE_REDIRECT_WARNINGS _f_i_l_e
             Write process warnings to the given _f_i_l_e instead of printing to
             stderr.  Equivalent to passing ----rreeddiirreecctt--wwaarrnniinnggss _f_i_l_e on the
             command line.

     NODE_REPL_HISTORY _f_i_l_e
             Path to the _f_i_l_e used to store persistent REPL history.  The
             default path is ~~//..nnooddee__rreeppll__hhiissttoorryy, which is overridden by this
             variable.  Setting the value to an empty string ("" or " ") will
             disable persistent REPL history.

     NODE_REPL_EXTERNAL_MODULE _f_i_l_e
             Path to a Node.js module which will be loaded in place of the
             built-in REPL.  Overriding this value to an empty string (`''`)
             will use the built-in REPL.

     NODE_SKIP_PLATFORM_CHECK
             When set to _1, the check for a supported platform is skipped
             during Node.js startup.  Node.js might not execute correctly.
             Any issues encountered on unsupported platforms will not be
             fixed.

     NODE_TLS_REJECT_UNAUTHORIZED
             When set to _0, TLS certificate validation is disabled.

     NODE_V8_COVERAGE _d_i_r
             When set, Node.js writes JavaScript code coverage information to
             _d_i_r.

     OPENSSL_CONF _f_i_l_e
             Load an OpenSSL configuration file on startup.  Among other uses,
             this can be used to enable FIPS-compliant crypto if Node.js is
             built with ..//ccoonnffiigguurree ----ooppeennssssll--ffiippss.

             If the ----ooppeennssssll--ccoonnffiigg command-line option is used, this
             environment variable is ignored.

     SSL_CERT_DIR _d_i_r
             If ----uussee--ooppeennssssll--ccaa is enabled, this overrides and sets OpenSSL's
             directory containing trusted certificates.

     SSL_CERT_FILE _f_i_l_e
             If ----uussee--ooppeennssssll--ccaa is enabled, this overrides and sets OpenSSL's
             file containing trusted certificates.

     TZ      Specify the timezone configuration.

     UV_THREADPOOL_SIZE _s_i_z_e
             Sets the number of threads used in libuv's threadpool to _s_i_z_e.

BBUUGGSS
     Bugs are tracked in GitHub Issues: hhttttppss::////ggiitthhuubb..ccoomm//nnooddeejjss//nnooddee//iissssuueess

CCOOPPYYRRIIGGHHTT
     Copyright Node.js contributors.  Node.js is available under the MIT
     license.

     Node.js also includes external libraries that are available under a
     variety of licenses.  See
     hhttttppss::////ggiitthhuubb..ccoomm//nnooddeejjss//nnooddee//bblloobb//HHEEAADD//LLIICCEENNSSEE for the full license
     text.

SSEEEE AALLSSOO
     Website: hhttttppss::////nnooddeejjss..oorrgg//

     Documentation: hhttttppss::////nnooddeejjss..oorrgg//aappii//

     GitHub repository and issue tracker: hhttttppss::////ggiitthhuubb..ccoomm//nnooddeejjss//nnooddee

     IRC (general questions): lliibbeerraa..cchhaatt ##nnooddee..jjss (unofficial)

AAUUTTHHOORRSS
     Written and maintained by 1000+ contributors:
     hhttttppss::////ggiitthhuubb..ccoomm//nnooddeejjss//nnooddee//bblloobb//HHEEAADD//AAUUTTHHOORRSS

                                     2018
